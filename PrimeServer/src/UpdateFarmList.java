
public class UpdateFarmList implements Runnable {

	@Override
	public void run() {
		// TODO Auto-generated method stub
		while (true) {
			/*try {
				Thread.sleep(10000);
			} catch (InterruptedException e) {
				// TODO Auto-generated catch block
				e.printStackTrace();
			}
			for (int i = 0; i < FarmProvider.listoffarms.size(); i++) {
				try {
					FarmId sockettemp = FarmProvider.listoffarms.get(i);
					sockettemp.sendToFarm("cpu");
					sockettemp.cpuusages = Integer.parseInt(sockettemp.receiveFromFarm());

				} catch (Exception e) {
					continue; // TODO Auto-generated catch block
				}

			}
			System.out.println("Updater the farm list finished ");
	*/	}

	}


}